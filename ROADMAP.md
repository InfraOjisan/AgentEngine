# ロードマップ

現時点（v0.2、フェーズ型オーケストレーション追加後）での次の一手と、まだ決めていないことのメモ。

## 優先順位

1. **次: ハーネス側のネイティブセッション継続**
   `claude --resume` / `codex exec resume` / `qwen --resume` / `pi --resume` / `agy --continue`
   など、各ハーネスが持つセッション継続の仕組みを使えるようにする。現状は毎ターン全文脈を
   `buildPromptForTurn()`で再送するステートレス方式（`meta.json`にセッションIDを保存する
   下地はあるが未使用）。ネイティブ継続に切り替えられればコンテキストコスト削減・長時間
   セッションの安定性向上が見込める。

2. **その前に検討: 軽量な状態確認コマンド**
   「どのロールにどのハーネスが割り当てられているか」は`team list`で見えるが、「今どのセッションの
   どのフェーズ/どのエージェントが実行中か」を一目で見たい、という動機。設定ファイルと
   `.agentengine/sessions/<...>/`を都度覗けば分かる話ではあるので優先度は高くないが、
   本格ダッシュボードよりは手前でやる価値がありそう。

3. **最後: 本格ダッシュボード**
   分割ペイン・コスト集計・複数セッション横断表示など。`SessionBus`イベント経由で疎結合に
   拡張できる設計にはしてある。

## 検討中・未決定

- **CI/CD統合**: 同じUI/フローの中でCI/CDパイプラインの実行・結果表示まで統合するかは未定。
  「ここまでシェルに近い位置にいるなら、素直にコマンドを叩けばよいのでは」という懐疑も込みで保留。

## メモ

機能追加を進める前に、「AgentEngineが具体的に何をやる/やらないツールなのか」というポリシーと
スコープを先に明確化すべき、という認識で止まっている（2026-08-31時点）。
